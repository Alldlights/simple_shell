#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

void print_string(char *str);
size_t _strlen(const char *str);
char *_strcpy(char *dest, char *src);

#endif /* MAIN_H */
